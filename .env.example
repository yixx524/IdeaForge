# 复制为 .env 并填入真实值（.env 已被 gitignore，不会提交）
# 本地开发：不设置 SPRING_PROFILES_ACTIVE，默认连 application.yml 中的虚拟机数据库
DEEPSEEK_API_KEY=sk-your-key-here
DB_PASSWORD=root

# 云服务器部署见 deploy/env.example（需设置 SPRING_PROFILES_ACTIVE=prod）
